/**
 * MSW HANDLERS (UNUSED)
 * 
 * This file exists for compatibility but is not used
 * We're using a custom GraphQL interceptor instead due to MSW cookie issues
 */

// Empty handlers array for compatibility
export const handlers: never[] = [] 